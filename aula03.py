#import math
from math import sqrt

num = 17
raiz = sqrt(num)
print(f"A raiz de {num} e {raiz:.2f}")

import random

num_rand = random.random()
print(num_rand*10)

num_rand_int = random.radint(1, 60)
prin(num_rand_int)